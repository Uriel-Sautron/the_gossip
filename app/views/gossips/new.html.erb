<h1>The Gossip <span class="badge badge-info">New</span></h1>
<p>Ici tu peut tout nous racontez !</p>


  <div class="form-group">


  <%= form_tag url_for(action: 'create'), method: 'post' do %>
    <input type="hidden" name="authenticity_token" value=<%= form_authenticity_token %> >
  <div class='form-group' >
    <p class="h3"><%= label_tag 'Title' %></p>
    <%= text_field_tag 'title', @gossip.title, class: 'form-control', placeholder: 'Entrer votre titre'  %>
  </div>
    <br /> <br />
  <div class='form-group'>
    <p class="h3"><%= label_tag 'Content' %></p>
    <%= text_area_tag 'content', @gossip.content, class: 'form-control', placeholder: 'Entrer votre contenue' %>
  </div>
    <br /> <br />

    <%= submit_tag "Create gossip", class: 'btn btn-primary' %>
  <% end %>
  
  </div>

<% if @gossip.errors.any? %>
  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
  <ul>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
  <% end %>
  </ul>
<% end %>

